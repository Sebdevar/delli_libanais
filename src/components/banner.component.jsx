import React from 'react';
import styled from 'styled-components';

const Banner = () => (
  <StyledBanner>test</StyledBanner>
);

const StyledBanner = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
`;
export default Banner;
